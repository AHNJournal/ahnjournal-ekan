langcode: en
status: true
dependencies:
  config:
    - group.type.publisher
  module:
    - user
id: publisher-group_membership
label: 'Publisher: Group membership'
description: 'Adds users to groups as members.'
group_type: publisher
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
